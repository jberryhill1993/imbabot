# Runtime dependencies (API backend — the recommended path)
requests>=2.31
keyring>=24.0        # OS keychain for the API key; falls back to a 0600 file
tzdata>=2024.1       # IANA timezone DB (needed for zoneinfo on Windows)
pywebview>=5.0       # glass web UI (WebView2/EdgeChromium on Windows); --classic uses Tkinter

# Browser-automation backend (drives your installed Google Chrome).
# selenium bundles into the .exe/.app and is the default browser driver; it uses
# the bundled selenium-manager to fetch a matching chromedriver at first run.
selenium>=4.20
# playwright is an ALTERNATIVE browser driver for running from source (not bundled).
#   pip install playwright && playwright install chromium
playwright>=1.40

# Build-time only (packaging the .exe). Install in your build environment:
#   pip install pyinstaller
# pyinstaller>=6.0
